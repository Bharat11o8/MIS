# ── Frontend ──────────────────────────────────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local

# ── Backend ───────────────────────────────────────────────────────────────────
backend/venv/
**/__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.pytest_cache/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.local
.env.*.local
backend/.env

# ── Local data sheets (customer data — never commit) ─────────────────────────
Local_sheets/

# ── Local-only docs & scripts (contain VPS/internal details — never commit) ───
# None of these holds a password, but between them they name the VPS host, its
# IP and SSH user, the service account, a Drive folder id and staff email
# addresses. In a public repo that is a free reconnaissance pack: it tells a
# stranger exactly who to phish and where to knock.
DEPLOY.md
DEV_AND_DEPLOY.md
DEPLOYMENT.md
DRIVE_DELEGATION_SETUP.md
VISIT_LOG_PHOTO_SETUP.md
tunnel-vps-db.ps1
CLAUDE.md
# ── IDE & tooling ─────────────────────────────────────────────────────────────
.vscode/
.idea/
.claude/
*.suo
*.user

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
Assets

# ── Local ops notes (not for the repo) ───────────────────────────────────────
AI_CHATBOT_PLAN.md
SSH_KEY_SETUP.md